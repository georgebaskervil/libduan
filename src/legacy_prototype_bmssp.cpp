// Legacy prototype file stub. Should NOT be part of any target; kept as an
// inert stub so accidental inclusion doesn't break incremental builds while
// we hunt stale CMake references. Remove after confirming build system clean.
namespace bmssp {
static int legacy_stub_noop = 0;
}
